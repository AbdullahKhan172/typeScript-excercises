let famouse_person: string = "Muhammad Ali Jinnah";

let message: string = `${famouse_person} once said, “Think a hundred times before you take a decision, but once that decision is taken, stand by it as one man.” `;

console.log(message);
